<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_below="@id/fab"
    android:id="@+id/row_file_type">

    <TextView
        android:id="@+id/file_type_title"
        android:alpha="0.54"
        android:text="Files"
        android:textColor="@color/material_black"
        android:textSize="@dimen/file_description_text_size"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:layout_marginLeft="@dimen/file_type_padding_left"
        android:layout_marginRight="@dimen/activity_horizontal_margin"/>

    <View
        android:alpha="0.12"
        android:background="@color/material_black"
        android:id="@+id/file_type_divider"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@id/file_type_title"
        android:layout_alignParentRight="true"
        android:layout_width="match_parent"
        android:layout_height="0.55dp"/>

</RelativeLayout>